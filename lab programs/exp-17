from sklearn.cluster import KMeans
import pandas as pd

data = {
'Amount':[100,200,150,400,500,450],
'Visits':[5,3,4,10,12,11]
}

df = pd.DataFrame(data)

kmeans = KMeans(n_clusters=2)

df['Cluster'] = kmeans.fit_predict(df)

print(df)
